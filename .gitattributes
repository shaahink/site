# Normalize line endings: LF in the repo, native in working copies.
* text=auto eol=lf

# Binary assets — never touch.
*.pdf   binary
*.woff  binary
*.woff2 binary
*.png   binary
*.jpg   binary
*.ico   binary
